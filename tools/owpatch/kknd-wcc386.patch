diff --git a/bld/cg/c/bldsel.c b/bld/cg/c/bldsel.c
index 6dc64e43..2e0ace69 100644
--- a/bld/cg/c/bldsel.c
+++ b/bld/cg/c/bldsel.c
@@ -31,6 +31,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "cgmem.h"
 #include "seldef.h"
@@ -445,7 +446,8 @@ static void DoBinarySearch( an node, const select_list *list, const type_def *ti
             return;
         }
     }
-    if( hi == mid + 1 && U64isEq( mid_list->next->low, mid_list->next->high ) ) {
+    if( hi == mid + 1 && U64isEq( mid_list->next->low, mid_list->next->high )
+      && getenv( "KKND_LINSEL" ) != NULL ) {  /* KKND: Watcom 10.x had no linear shortcut */
         /*
          * a linear sequence for three different non-sequential cases where
          * c1<c2<c3, looks like:
diff --git a/bld/cg/c/blktrim.c b/bld/cg/c/blktrim.c
index 5b346d0e..6b6571c0 100644
--- a/bld/cg/c/blktrim.c
+++ b/bld/cg/c/blktrim.c
@@ -31,6 +31,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "cgmem.h"
 #include "data.h"
@@ -256,7 +257,10 @@ static  bool    Retarget( block *blk )
     blk->input_edges = NULL;
     for( ; edge != NULL; edge = next ) {
         next = edge->next_source;
-        if( _IsBlkAttr( edge->source, BLK_SELECT | BLK_LABEL_RETURN ) ) {
+        if( _IsBlkAttr( edge->source, BLK_SELECT | BLK_LABEL_RETURN )
+          || ( getenv( "KKND_NORETRET" ) != NULL
+            && _IsBlkAttr( edge->source, BLK_CONDITIONAL )
+            && _IsBlkAttr( target, BLK_RETURN ) ) ) {
             success = false;   /* let the optimizer do it later on*/
             edge->next_source = blk->input_edges;
             blk->input_edges = edge;
diff --git a/bld/cg/c/cachecon.c b/bld/cg/c/cachecon.c
index e54b1d0d..ca9f15fe 100644
--- a/bld/cg/c/cachecon.c
+++ b/bld/cg/c/cachecon.c
@@ -32,6 +32,8 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
+#include <stdio.h>
 #include "coderep.h"
 #include "stackcg.h"
 #include "makeins.h"
@@ -197,6 +199,68 @@ void    ConstToTemp( block *pre, block *head, block*(*next)(block*) )
     }
 }
 
+static  name    *KKNDTemps[64];
+static  int     KKNDNumTemps;
+
+bool    KKNDIsGlobalConstTemp( name *op )
+{
+    int     i;
+
+    for( i = 0; i < KKNDNumTemps; ++i ) {
+        if( KKNDTemps[i] == op ) {
+            return( true );
+        }
+    }
+    return( false );
+}
+
+
+void    KKNDConstToTempFunc( void )
+/**********************************
+    KKND experiment: Watcom 10.x stored constants to global (N_MEMORY) variables through a
+    register: mov ebx,1; xor edx,edx; mov [A],ebx; mov [B],edx. Struct-field stores keep the
+    immediate. Cache every constant that is moved into a global in a temp, for those moves only.
+*/
+{
+    name                *cons;
+    name                *temp;
+    block               *blk;
+    instruction         *ins;
+
+    KKNDNumTemps = 0;
+    for( cons = Names[N_CONSTANT]; cons != NULL; cons = cons->n.next_name ) {
+        if( cons->c.const_type != CONS_ABSOLUTE )
+            continue;
+        temp = NULL;
+        for( blk = HeadBlock; blk != NULL; blk = blk->next_block ) {
+            for( ins = blk->ins.head.next; ins->head.opcode != OP_BLOCK; ins = ins->head.next ) {
+                if( ins->head.opcode != OP_MOV || ins->operands[0] != cons )
+                    continue;
+                if( getenv( "KKND_DEBUG" ) != NULL )
+                    fprintf( stderr, "KKND mov const %ld class %d resclass %d\n", (long)cons->c.lo.u.int_value, ins->type_class, ins->result->n.class );
+                if( ins->result->n.class != N_MEMORY )
+                    continue;
+                if( ins->type_class != WD && ins->type_class != SW
+                  && ins->type_class != U4 && ins->type_class != I4 )
+                    continue;
+                if( temp == NULL ) {
+                    temp = AllocTemp( ins->type_class );
+                    temp->t.temp_flags |= CONST_TEMP;
+                    temp->v.symbol = cons;
+                    temp->v.usage |= USE_IN_ANOTHER_BLOCK;
+                    if( KKNDNumTemps < 64 )
+                        KKNDTemps[KKNDNumTemps++] = temp;
+                    PrefixIns( HeadBlock->ins.head.next, MakeMove( cons, temp, ins->type_class ) );
+                }
+                if( ins->type_class == temp->n.type_class ) {
+                    ins->operands[0] = temp;
+                }
+            }
+        }
+    }
+}
+
+
 void            MemConstTemp( conflict_node *conf )
 /**************************************************
 
diff --git a/bld/cg/c/cse.c b/bld/cg/c/cse.c
index 721fa0c6..bcb4e28a 100644
--- a/bld/cg/c/cse.c
+++ b/bld/cg/c/cse.c
@@ -31,6 +31,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "zoiks.h"
 #include "tree.h"
@@ -1604,7 +1605,7 @@ bool    CommonSex( bool leave_indvars_alone )
         change = false;
         if( DoPropagateMoves() )
             change = true;
-        if( StretchEdges() ) {
+        if( getenv( "KKND_STRETCH" ) != NULL && StretchEdges() ) {  /* KKND: off, Watcom 10.x did not thread these */
             change = true;
             loops_killed = true;
             CleanPartition();
diff --git a/bld/cg/c/generate.c b/bld/cg/c/generate.c
index a7f606be..27aed084 100644
--- a/bld/cg/c/generate.c
+++ b/bld/cg/c/generate.c
@@ -31,6 +31,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "memcheck.h"
 #include "data.h"
@@ -722,6 +723,11 @@ void    Generate( bool routine_done )
         Panic( false );
         return;
     }
+    if( getenv( "KKND_CONSTREG" ) != NULL ) {
+        extern void KKNDConstToTempFunc( void );
+        KKNDConstToTempFunc();
+        FindReferences();
+    }
     MakeConflicts();
     if( _IsModel( CGSW_GEN_LOOP_OPTIMIZATION ) ) {
         SplitVars();
diff --git a/bld/cg/c/loopopts.c b/bld/cg/c/loopopts.c
index 9a812700..df76f781 100644
--- a/bld/cg/c/loopopts.c
+++ b/bld/cg/c/loopopts.c
@@ -31,6 +31,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "indvars.h"
 #include "cgmem.h"
@@ -3420,6 +3421,28 @@ static  void    DupNoncondInstrs( block *cond_blk, instruction *cond, block *pre
 }
 #endif
 
+static  bool    KKNDSingleExit( block *cond_blk )
+/* true if no loop block other than cond_blk leaves the loop */
+{
+    block       *blk;
+    block_num   i;
+
+    for( blk = Loop; blk != NULL; blk = blk->u.loop ) {
+        if( blk == cond_blk )
+            continue;
+        if( _IsBlkAttr( blk, BLK_RETURN ) )
+            return( false );
+        for( i = 0; i < blk->targets; ++i ) {
+            if( (blk->edge[i].flags & BEF_DEST_IS_BLOCK) == 0 )
+                return( false );
+            if( !_IsBlkAttr( blk->edge[i].destination.u.blk, BLK_IN_LOOP ) )
+                return( false );
+        }
+    }
+    return( true );
+}
+
+
 static  bool    TwistLoop( block_list *header_list, bool unroll )
 /****************************************************************
  * Try to fall into loops without testing the condition the first
@@ -3551,6 +3574,10 @@ static  bool    TwistLoop( block_list *header_list, bool unroll )
             PointEdge( edge, cond_blk->edge[1].destination.u.blk );
             MoveDownLoop( cond_blk );
             new_head = loop_edge->destination.u.blk;
+        } else if( getenv( "KKND_NOROT" ) == NULL && KKNDSingleExit( cond_blk ) ) {
+            /* KKND: Watcom 10.x enters a single-exit loop at its bottom test */
+            MoveDownLoop( cond_blk );
+            new_head = loop_edge->destination.u.blk;
         }
         if( new_head != NULL
           && new_head != Head ) {
diff --git a/bld/cg/c/multiply.c b/bld/cg/c/multiply.c
index 76c9e5da..abc589fa 100644
--- a/bld/cg/c/multiply.c
+++ b/bld/cg/c/multiply.c
@@ -31,6 +31,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "data.h"
 #include "makeins.h"
@@ -74,6 +75,20 @@ static  int     Factor( uint_32 num, int *cost )
     if( num == 0xFFFFFFFF )
         return( i );
 
+    if( getenv( "KKND_OWFACTOR" ) == NULL ) {
+        /* KKND: Watcom 10.x shifts out the trailing power of two last: n*56 = ((n<<3)-n)<<3 */
+        shlcnt = 0;
+        while( (num & 1) == 0 ) {
+            ++shlcnt;
+            num >>= 1;
+        }
+        if( shlcnt != 0 ) {
+            if( --i < 0 )
+                return( MAXOPS );
+            Ops[i].op = DO_SHL;
+            Ops[i].cnt = shlcnt;
+        }
+    }
     test = num >> 1;
     do {
         pow2 = 1;
diff --git a/bld/cg/c/regalloc.c b/bld/cg/c/regalloc.c
index 90c18577..57b16375 100644
--- a/bld/cg/c/regalloc.c
+++ b/bld/cg/c/regalloc.c
@@ -31,6 +31,8 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
+#include <stdio.h>
 #include "coderep.h"
 #include "zoiks.h"
 #include "data.h"
@@ -922,6 +924,8 @@ static  void    NeighboursUse( conflict_node *conf )
 }
 
 
+extern bool KKNDIsGlobalConstTemp( name * );
+
 static hw_reg_set   GiveBestReg( conflict_node *conf, reg_tree *tree,
                                      hw_reg_set except, bool needs_one )
 /*************************************************************************
@@ -986,6 +990,9 @@ static hw_reg_set   GiveBestReg( conflict_node *conf, reg_tree *tree,
         all_true = true;
         for( possible = tree->regs; !HW_CEqual( *possible, HW_EMPTY ); ++possible ) {
             reg = *possible;
+            if( conf->name->n.class == N_TEMP && (conf->name->t.temp_flags & CONST_TEMP)
+              && HW_COvlap( reg, HW_EAX ) && KKNDIsGlobalConstTemp( conf->name ) )
+                continue;   /* KKND experiment: Watcom 10.x never put a cached constant in EAX */
             if( !HW_Ovlap( reg, conf->with.regs )
               && !HW_Ovlap( reg, except ) ) {
                 greed = TooGreedy( conf, reg, tree->temp );
@@ -1207,6 +1214,20 @@ static  void    SortConflicts( void )
     Sort the conflicts in order of descending savings.
 */
 {
+    if( getenv( "KKND_CONFREV" ) != NULL ) {
+        /* KKND experiment: reverse the order of conflicts with equal savings */
+        conflict_node   *conf;
+        conflict_node   *next;
+        conflict_node   *rev;
+
+        rev = NULL;
+        for( conf = ConfList; conf != NULL; conf = next ) {
+            next = conf->next_conflict;
+            conf->next_conflict = rev;
+            rev = conf;
+        }
+        ConfList = rev;
+    }
     ConfList = SortList( ConfList, offsetof( conflict_node, next_conflict ), ConfBefore );
 }
 
@@ -1242,6 +1263,18 @@ static  enum allocation_state    AssignConflicts( void )
         }
     }
     ConstSavings();
+    for( conf = ConfList; conf != NULL; conf = conf->next_conflict ) {
+        /* KKND: a cached global-store constant always wants a register */
+        if( conf->savings == 0 && conf->start_block != NULL && conf->name->n.class == N_TEMP
+          && KKNDIsGlobalConstTemp( conf->name ) ) {
+            conf->savings = 1;
+        }
+        if( getenv( "KKND_DEBUG" ) != NULL && conf->name->n.class == N_TEMP ) {
+            fprintf( stderr, "KKND conf temp %p mine %d savings %ld const %d\n", (void *)conf->name,
+                KKNDIsGlobalConstTemp( conf->name ), (long)conf->savings,
+                (conf->name->t.temp_flags & CONST_TEMP) != 0 );
+        }
+    }
 
     SortConflicts();
     state = ALLOC_BITS;
diff --git a/bld/cg/intel/386/c/386rgtbl.c b/bld/cg/intel/386/c/386rgtbl.c
index 3ea68d69..f534ca05 100644
--- a/bld/cg/intel/386/c/386rgtbl.c
+++ b/bld/cg/intel/386/c/386rgtbl.c
@@ -31,6 +31,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "data.h"
 #include "i87data.h"
@@ -285,11 +286,11 @@ static const hw_reg_set     ABCDRegs[] = {
     HW_D_1( HW_ECX ),
     HW_D_1( HW_EMPTY )
 };
-static const hw_reg_set     DoubleRegs[] = {
+static hw_reg_set           DoubleRegs[] = {   /* KKND: Watcom 10.x order, EBX before ECX */
     HW_D_1( HW_EAX ),
     HW_D_1( HW_EDX ),
-    HW_D_1( HW_ECX ),
     HW_D_1( HW_EBX ),
+    HW_D_1( HW_ECX ),
     HW_D_1( HW_ESI ),
     HW_D_1( HW_EDI ),
     HW_D_1( HW_EBP ),
@@ -636,6 +637,26 @@ void            InitRegTbl( void )
  * Initialize the tables.
  */
 {
+    if( getenv( "KKND_ECXFIRST" ) != NULL ) {   /* KKND: stock OW order */
+        HW_CAsgn( DoubleRegs[2], HW_ECX );
+        HW_CAsgn( DoubleRegs[3], HW_EBX );
+    }
+    if( getenv( "KKND_REGORDER" ) != NULL ) {
+        /* KKND experiment: KKND_REGORDER=dbcasi (letters a,b,c,d,s,i) sets the first six */
+        const char  *o = getenv( "KKND_REGORDER" );
+        int         k;
+
+        for( k = 0; k < 6 && o[k] != '\0'; ++k ) {
+            switch( o[k] ) {
+            case 'a': HW_CAsgn( DoubleRegs[k], HW_EAX ); break;
+            case 'b': HW_CAsgn( DoubleRegs[k], HW_EBX ); break;
+            case 'c': HW_CAsgn( DoubleRegs[k], HW_ECX ); break;
+            case 'd': HW_CAsgn( DoubleRegs[k], HW_EDX ); break;
+            case 's': HW_CAsgn( DoubleRegs[k], HW_ESI ); break;
+            case 'i': HW_CAsgn( DoubleRegs[k], HW_EDI ); break;
+            }
+        }
+    }
     if( _FPULevel( FPU_87 ) ) {
         HW_CAsgn( STParmReg[Max87Stk], HW_EMPTY );
     }
diff --git a/bld/cg/intel/c/x86enc2.c b/bld/cg/intel/c/x86enc2.c
index cfc28780..6545a667 100644
--- a/bld/cg/intel/c/x86enc2.c
+++ b/bld/cg/intel/c/x86enc2.c
@@ -32,6 +32,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "optmain.h"
 #include "cgmem.h"
@@ -137,6 +138,8 @@ unsigned DepthAlign( unsigned depth )
     }
     if( OptForSize > 0 )
         return( 1 );
+    if( getenv( "KKND_LOOPALIGN" ) == NULL )
+        return( 1 );    /* KKND: Watcom 10.x never padded loop heads or the code after a jump table */
     if( _CPULevel( CPU_486 ) ) {
         if( depth == PROC_ALIGN || depth == DEEP_LOOP_ALIGN )
             return( 16 );
diff --git a/bld/cg/intel/c/x86mul.c b/bld/cg/intel/c/x86mul.c
index 37aaf4e8..258b7341 100644
--- a/bld/cg/intel/c/x86mul.c
+++ b/bld/cg/intel/c/x86mul.c
@@ -31,6 +31,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "data.h"
 #include "multiply.h"
@@ -48,7 +49,14 @@ int     MulCost( uint_32 num )
      * is less than the number of cycles it takes to execute, to give slight
      * preference to multiply versus a shift/add sequence.
      */
-    if( _CPULevel( CPU_686 ) ) {
+    if( getenv( "KKND_OWMUL" ) == NULL && _CPULevel( CPU_386 ) ) {
+        /* KKND: Watcom 10.x used the 386/486 early-out cost for every 32-bit target */
+        cost = 4;
+        while( num ) {
+            cost++;
+            num >>= 1;
+        }
+    } else if( _CPULevel( CPU_686 ) ) {
         /* On the Pentium Pro and up, multiply is pretty cheap. */
         cost = 3;
     } else if( _CPULevel( CPU_586 ) ) {
diff --git a/bld/cg/intel/c/x86regsv.c b/bld/cg/intel/c/x86regsv.c
index a5a4fdb3..9cb37ab7 100644
--- a/bld/cg/intel/c/x86regsv.c
+++ b/bld/cg/intel/c/x86regsv.c
@@ -32,6 +32,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "data.h"
 #include "savings.h"
@@ -93,6 +94,8 @@ void    InitWeights( uint size )
 }
 
 
+extern bool KKNDIsGlobalConstTemp( name * );
+
 bool    WorthProlog( conflict_node *conf, hw_reg_set reg )
 /*******************************************************************
     decide if the savings associated with giving conflict "conf"
@@ -116,6 +119,8 @@ bool    WorthProlog( conflict_node *conf, hw_reg_set reg )
     }
     op = conf->name;
     savings = conf->savings;
+    if( _ConstTemp( op ) && getenv( "KKND_CONSTREG" ) != NULL && KKNDIsGlobalConstTemp( op ) )
+        return( true );
     if( _ConstTemp( op ) ) {
         /* adjust for the initial load */
         cost += Weight( Save.load_cost[type_class] + Save.def_save[type_class], conf->start_block );
diff --git a/bld/cg/intel/c/x86segs.c b/bld/cg/intel/c/x86segs.c
index d5bb49a4..e28fa262 100644
--- a/bld/cg/intel/c/x86segs.c
+++ b/bld/cg/intel/c/x86segs.c
@@ -66,7 +66,11 @@ hw_reg_set      CalcSegment( cg_sym_handle sym, cg_class class ) {
     segment_id  segid;
     hw_reg_set  *reg;
 
-    if( class == CG_TBL || class == CG_VTB || class == CG_CLB ) {
+    if( class == CG_TBL ) {
+        /* KKND: Watcom 10.x addressed switch jump tables through CS even in flat model */
+        return( HW_CS );
+    }
+    if( class == CG_VTB || class == CG_CLB ) {
         if( _IsTargetModel( CGSW_X86_FLAT_MODEL ) )
             return( HW_DS );
         return( HW_CS );
diff --git a/bld/cg/intel/c/x86sel.c b/bld/cg/intel/c/x86sel.c
index 1de30dab..6032de50 100644
--- a/bld/cg/intel/c/x86sel.c
+++ b/bld/cg/intel/c/x86sel.c
@@ -32,6 +32,7 @@
 
 
 #include "_cgstd.h"
+#include <stdlib.h>
 #include "coderep.h"
 #include "cgmem.h"
 #include "tree.h"
@@ -113,8 +114,12 @@ static cost_val Balance( uint_32 size, uint_32 time )
     byte        opt_size;
 
     opt_size = OptForSize;
-    if( opt_size < 25 ) {
-        opt_size = 25;
+    if( getenv( "KKND_SWOPT" ) != NULL ) {
+        /* KKND experiment: override the size/time balance used to pick a switch strategy */
+        opt_size = (byte)atoi( getenv( "KKND_SWOPT" ) );
+    } else if( opt_size < 15 ) {
+        /* KKND: retail Watcom 10.x switch choices fit a floor in [13,19] (OW uses 25) */
+        opt_size = 15;
     }
     balance = ( size * opt_size + 10 * time * ( 100 - opt_size ) ) / 100;
     if( (int_32)balance < 0 || balance > MAX_COST )
